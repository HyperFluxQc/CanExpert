"""TestExpert: loads an ECU's diagnostic description (CDD, ODX/PDX, JSON) and runs generated tests against the
ECU to see whether it keeps the UDS (ISO 14229-1) rules - what Vector DiVa does. Its own program
(test_expert.py), on CAN Expert's bus, UDS, test runner and reports."""
